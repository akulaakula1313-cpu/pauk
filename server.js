const http=require('http'),fs=require('fs'),path=require('path');
const ROOT=__dirname,PORT=process.env.PORT||3000,MIME={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.webp':'image/webp'};
function safePath(url){const decoded=decodeURIComponent(url.split('?')[0]);const rel=decoded==='/'?'/index.html':decoded;const full=path.normalize(path.join(ROOT,rel));return full===ROOT||full.startsWith(ROOT+path.sep)?full:null}
http.createServer((req,res)=>{try{const file=safePath(req.url||'/');if(!file)return res.writeHead(403).end('Forbidden');fs.stat(file,(err,st)=>{if(err||!st.isFile())return res.writeHead(404).end('Not found');res.setHeader('Cache-Control','no-cache');res.writeHead(200,{'Content-Type':MIME[path.extname(file).toLowerCase()]||'application/octet-stream'});fs.createReadStream(file).pipe(res)})}catch(e){res.writeHead(400).end('Bad request')}}).listen(PORT,()=>console.log(`SANI GROUP Spider: http://localhost:${PORT}`));
