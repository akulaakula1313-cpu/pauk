# SANI GROUP Spider

## Render

This project is ready for Render as a Node Web Service.

- Build Command: `npm install`
- Start Command: `npm start`
- Node: 20+

The server uses `process.env.PORT`, so Render can assign the port automatically.
